[project]
name = "synchroni-sensor-sdk"
version = "2.1.1"
description = "SDK for interfacing with Synchroni BLE devices"
url = "https://github.com/SynchroniSI/synchroni-sensor-sdk"
authors = [
    {name = "it@synchroni.co"}
]
readme = "README.md"
requires-python = ">=3.10"

[tool.poetry]
packages = [
    { include = "sensor" },
    { include = "synchroni_sensor_sdk" },
]

[tool.poetry.dependencies]
numpy = ">=1.23.0"
bleak = ">=2.1.1,<3.0.0"
flatbuffers = ">=25.0.0"
bumble = {version = ">=0.0.233,<0.1.0", optional = true}

[tool.poetry.extras]
managed-usb = ["bumble"]

[tool.poetry.group.dev.dependencies]
ruff = ">=0.14.14,<0.15.0"
mypy = ">=1.19.1,<2.0.0"
pytest-asyncio = ">=1.3.0,<2.0.0"
typer = ">=0.15.0"

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"

[tool.ruff]
line-length = 120
target-version = "py312"
exclude = [
    ".venv",
    ".venv.local",
    "examples",
    "sensor",
]


[tool.ruff.lint]
select = [
    "E",  # pycodestyle errors
    "W",  # pycodestyle warnings
    "F",  # pyflakes
    "I",  # isort
    "B",  # flake8-bugbear
    "C4", # flake8-comprehensions
    "UP", # pyupgrade
    "ARG", # flake8-unused-arguments
    "SIM", # flake8-simplify
]
ignore = [
    "E501",  # line too long (handled by formatter)
    "B008",  # do not perform function calls in argument defaults
    "UP046", # Python 3.11 forces us to use Generic
]

[tool.ruff.format]
quote-style = "double"
indent-style = "space"

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
disallow_untyped_defs = true
disallow_incomplete_defs = true
check_untyped_defs = true
disallow_untyped_decorators = true
no_implicit_optional = true
warn_redundant_casts = true
warn_unused_ignores = true
warn_no_return = true
strict_equality = true
show_error_codes = true
files = ["entry", "synchroni_tools", "v2_comparison"]
exclude = [
    "^.venv/",
    "^.venv.local/",
    "^dist/",
]

[[tool.mypy.overrides]]
module = [
    "uuid_extensions",
    "scipy",
    "scipy.*",
]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["bumble", "bumble.*", "usb1", "usb1.*"]
ignore_missing_imports = true

[[tool.mypy.overrides]]
module = ["sensor", "sensor.*"]
ignore_errors = true
ignore_missing_imports = true

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests", "v2_comparison/tests"]
python_files = ["test_*.py"]
python_classes = ["Test*"]
python_functions = ["test_*"]
